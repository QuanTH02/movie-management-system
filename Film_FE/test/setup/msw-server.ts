/**
 * MSW server setup for tests.
 * This creates a mock server that intercepts HTTP requests during testing.
 * Note: This file is used by jest.setup.js, which uses require() instead of import.
 */
export {};

